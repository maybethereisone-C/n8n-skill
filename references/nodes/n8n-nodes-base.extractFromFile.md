# Extract from File  (`n8n-nodes-base.extractFromFile`)

- typeVersion (max): **1.1**  | group: input  | trigger: no
- credentials: —
- operations: binaryToPropery, csv, fromIcs, fromJson, html, ods, pdf, rtf, text, xls, xlsx, xml

## Parameters

| name | displayName | type | default | req | gated by |
|---|---|---|---|---|---|
| `operation` | Operation | options | csv |  |  |
