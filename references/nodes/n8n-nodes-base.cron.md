# Cron  (`n8n-nodes-base.cron`)

- typeVersion (max): **1**  | group: trigger,schedule  | trigger: no
- credentials: —

## Parameters

| name | displayName | type | default | req | gated by |
|---|---|---|---|---|---|
| `notice` | This workflow will run on the schedule you define here once you publish it.<br><br>For testing, you can also trigger it manually: by going back to the canvas and clicking 'execute workflow' | notice |  |  |  |
| `triggerTimes` | Trigger Times | fixedCollection | {} |  |  |
