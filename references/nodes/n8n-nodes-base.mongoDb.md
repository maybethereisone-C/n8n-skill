# MongoDB  (`n8n-nodes-base.mongoDb`)

- typeVersion (max): **1.3**  | group: input  | trigger: no
- credentials: —
- resources: document, searchIndexes
- operations: aggregate, createSearchIndex, delete, dropSearchIndex, find, findOneAndReplace, findOneAndUpdate, insert, listSearchIndexes, update, updateSearchIndex

## Parameters

| name | displayName | type | default | req | gated by |
|---|---|---|---|---|---|
| _(properties not statically extractable — confirm via n8n-mcp get_node)_ |||||| 
