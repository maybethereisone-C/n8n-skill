# Airtop  (`n8n-nodes-base.airtop`)

- typeVersion (max): **1.1**  | group: transform  | trigger: no
- credentials: airtopApi
- resources: agent, extraction, file, interaction, session, window
- operations: click, close, create, deleteFile, fill, get, getLiveView, getMany, getPaginated, hover, list, load, query, run, save, scrape, scroll, takeScreenshot, terminate, type, upload, waitForDownload

## Parameters

| name | displayName | type | default | req | gated by |
|---|---|---|---|---|---|
| `resource` | Resource | options | session |  |  |
