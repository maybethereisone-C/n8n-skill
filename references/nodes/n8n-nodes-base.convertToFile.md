# Convert to File  (`n8n-nodes-base.convertToFile`)

- typeVersion (max): **1.1**  | group: input  | trigger: no
- credentials: —
- operations: csv, html, iCal, ods, rtf, toBinary, toJson, toText, xls, xlsx

## Parameters

| name | displayName | type | default | req | gated by |
|---|---|---|---|---|---|
| `operation` | Operation | options | csv |  |  |
